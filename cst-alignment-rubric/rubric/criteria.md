# Rubric criteria

TODO — not yet written. Defines, per principle, a graded scale (not a single pass/fail check) plus an explicit "genuinely contested — route to human review" flag, so scoring never forces a false verdict onto a case like the ones in `known-tensions.md`.

Depends on `principles/*.yaml` being filled in first — a grading scale for an uncited principle has nothing to grade against.
