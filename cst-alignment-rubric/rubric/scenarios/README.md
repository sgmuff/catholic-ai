# Scenarios

Scenario prompts run against a target model or agent config by `eval/harness.py`. Empty for now — scenarios are written per principle as part of filling in `principles/*.yaml`'s `scenarios` field, then given full prompt text here.
