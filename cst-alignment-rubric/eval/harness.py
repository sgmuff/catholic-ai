"""Runs scenario prompts from rubric/scenarios/ against a target model or agent
config, scores responses via multi-rater consensus (see eval/raters/), and
writes an advisory report to eval/reports/ — never a pass/fail verdict.

Not implemented yet: this is v0.1 scaffolding. See the "Build eval harness
v0.1" Next Action in the project log.
"""


def run(scenario_dir: str, target_config: str) -> None:
    raise NotImplementedError("eval harness v0.1 has not been built yet")
