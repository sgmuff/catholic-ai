# Gateway hook

TODO — not yet written. Will document how `ai-acceptable-use-gateway` could import `principles/schema.yaml` as one input among several, not the sole determinant of whether a request is allowed.
