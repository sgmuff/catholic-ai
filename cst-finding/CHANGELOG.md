# Changelog

Versioned against magisterial developments — a new Vatican or USCCB statement touching AI, a revision to how a principle is interpreted — not against a sprint calendar. Every entry naming a change to `principles/` or `rubric/` links to its `docs/theological-review-log.md` entry.

## Unreleased

- **Renamed the project itself from `cst-alignment-rubric` to `cst-finding`, matching the skill.** Directory moved via `git mv`; `pyproject.toml`'s `name` updated; the root repo `README.md` and `.pre-commit-config.yaml` (the `pre-push` lint/test hook's id, name, and `-C` path) updated to point at the new path; this project's own `README.md` (title, directory tree, and a new sentence continuing the `cst-model-constitution` rename note above) and `CONTRIBUTING.md` title updated. No behavior, principle, or rubric content changed — naming only.
- **Renamed the skill from `rate-ai-against-cst` to `cst-finding`.** Shorter, and lifted from the skill's own language ("produces one advisory finding for one subject") rather than restating the action already implied by its description. `.claude/skills/rate-ai-against-cst/` moved to `.claude/skills/cst-finding/`; updated the `name:` frontmatter, the skill's self-reference to its own directory, `eval/assessment.py`'s docstring and `--input` help text, `pyproject.toml`'s comment, `README.md`, and `CHECKLIST.md`. No behavior, principle, or rubric content changed — naming only.
- **Renamed `docs/uscc-foundational-documents-index.md` to `docs/usccb-foundational-documents-index.md`** (the source is the USCCB, not "USCC" — a leftover from an early draft of the filename) **and rewrote it for readability.** The USCCB document list previously split into a short ✅ bullet list plus a dense, undated, semicolon-separated paragraph with two items pulled out of their natural position and awkward "(✅ above)" backreferences. Rewrote it as a single numbered list matching the Vatican-documents section's style and the source page's own order, with dates restored and ✅ annotations inline. No content or citation changed — a readability fix, not a theological one. Updated every reference to the old filename: `README.md`, `docs/ccc-citation-map.md`, `docs/theological-review-log.md`.
- **Closed out both remaining `docs/usccb-foundational-documents-index.md` candidates.** Extended case 3 of `rubric/known-tensions.md` (recidivism-risk scoring) with a "sharper instance, not equally contested" addendum on AI risk scores feeding capital-sentencing decisions specifically — grounded in CCC 2267 (the 2018 revision: the death penalty is "inadmissible") and USCCB's *A Culture of Life and the Penalty of Death* (2005), and deliberately written to *not* mark this instance `contested: true` on the same footing as the base case, since current teaching substantially resolves which side wins once execution isn't needed for public safety. Added a fifth case: AI-generated voter guides / candidate-scoring tools (`common-good` vs. `personalism`, with a `subsidiarity` connection), grounded in USCCB's *Forming Consciences for Faithful Citizenship* §34 and its warning against single-issue reduction. `docs/ccc-citation-map.md` and `docs/usccb-foundational-documents-index.md` updated; both now show every originally-flagged Tier B/C candidate resolved. **Unreviewed — beta**; see `docs/theological-review-log.md`.
- **Follow-up verification: both previously-flagged citations now checked directly against primary sources via paginated mirrors, and a fourth `known-tensions.md` case added.** Evangelium Vitae §57 (`dignity-and-inviolability-of-life.yaml`) verified via IntraText's `ENG0594/_P27.HTM` — vatican.va's own page was too long to reach it directly; no longer flagged as secondary-corroborated-only. Compendium §301-302 (just wage) verified via a chaptered mirror of the Compendium's official text and added to `non-negotiables.yaml`'s `systemic-wage-theft-by-design` item, which previously cited only CCC 1867 and Evangelium Vitae §3. Added a fourth `known-tensions.md` case — AI-driven workforce automation (`common-good`/`universal-destination-of-goods` vs. `personalism`/`preferential-option-for-the-poor`), grounded in USCCB's *Economic Justice for All* (1986), verified via a direct fetch (the USCCB's own PDF has no text layer; fetched a plain-text mirror instead). `docs/ccc-citation-map.md` and `docs/usccb-foundational-documents-index.md` updated accordingly. **Unreviewed — beta**; see `docs/theological-review-log.md`.
- **`rubric/known-tensions.md` now has three fully worked cases (Tier B of the USCCB foundational-documents pass), satisfying `CHECKLIST.md`'s go-live condition.** Fleshed out the existing preferential-option-vs-subsidiarity outreach-ranking seed case, and added two new ones: immigration-enforcement case prioritization (`solidarity`/`dignity-and-inviolability-of-life` vs. `common-good`/`subsidiarity`, grounded in CCC 2241 and USCCB's *Welcoming the Stranger Among Us*), and recidivism-risk scoring in sentencing/parole (`common-good` vs. `dignity-and-inviolability-of-life`/`preferential-option-for-the-poor`, grounded in USCCB's *Responsibility, Rehabilitation, and Restoration*). Added a BETA/unreviewed notice to the top of the file, matching every other file in `principles/` and `rubric/`. **Unreviewed — beta**; see `docs/theological-review-log.md`.
- **Added `docs/usccb-foundational-documents-index.md` (Tier C of the USCCB foundational-documents pass): a catalogue of the full USCCB list, marking what's cited (Tier A/B, above) and what's still uncited.** Not a citation source itself — explicitly marks everything in it as unverified until a future pass picks a document up properly. Names three candidates for a further known-tensions case: *Economic Justice for All*, *A Culture of Life and the Penalty of Death*, *Forming Consciences for Faithful Citizenship*.
- **Added one USCCB-foundational-documents-list encyclical citation to each of the eight `principles/*.yaml` files (Tier A of three).** The USCCB's [foundational documents](https://www.usccb.org/beliefs-and-teachings/what-we-believe/catholic-social-teaching/foundational-documents) page lists ~80 documents; curated into Tier A (this entry: one encyclical per principle), Tier B (USCCB pastoral statements for `rubric/known-tensions.md` worked cases, see below), and Tier C (the rest, catalogued but not cited — see below). Added: `personalism` → Gaudium et Spes §22; `common-good` → Gaudium et Spes §26; `subsidiarity` → Quadragesimo Anno §79-80; `social-justice` → Quadragesimo Anno §57-58; `solidarity` → Sollicitudo Rei Socialis §38; `preferential-option-for-the-poor` → Sollicitudo Rei Socialis §42; `universal-destination-of-goods` → Populorum Progressio §22-23; `dignity-and-inviolability-of-life` → Evangelium Vitae §57. All but one verified by direct fetch against vatican.va (Sollicitudo Rei Socialis §42 via a paginated IntraText mirror of the same official text, after vatican.va's own page proved too long to fetch that far); Evangelium Vitae §57 is corroborated by five-plus independent secondary sources rather than a direct fetch — flagged explicitly in `docs/ccc-citation-map.md` for a follow-up primary check rather than silently treated as equally verified. **Unreviewed — beta**; see `docs/theological-review-log.md`.
- **Added a Catechism of the Catholic Church citation to all eight `principles/*.yaml` files.** Each principle's `magisterial_citations` gained a CCC paragraph range, checked directly against its primary-source `vatican.va/content/catechism/en/...` page: `personalism` §1701-1709, `common-good` §1905-1912, `subsidiarity` §1883-1885, `solidarity` §1939-1942, `universal-destination-of-goods` §2402-2406, `preferential-option-for-the-poor` §2443-2449, `dignity-and-inviolability-of-life` §2258/§2270-2275/§2276-2279, `social-justice` §1928-1942. See `docs/ccc-citation-map.md` for the full mapping and location detail. Each file's header comment updated to note the CCC citation was added after the original Compendium/MH content. **Unreviewed — beta**; see `docs/theological-review-log.md`.
- **Fixed: bright-line citations are now per-item, not file-wide.** `principles/non-negotiables.yaml`'s citations moved from a single top-level `grounding` list (rendered in full under *every* match, regardless of which item matched) to a `citations` list per item, matching how a graded principle's `magisterial_citations` already works. Surfaced by testing the wage-theft item added below: its report was showing the unrelated abortion/euthanasia citations too. `eval/principles.py`'s `NonNegotiable` now carries `citations`; `load_non_negotiables_grounding` was removed; `eval/assessment.py` reads the matched item's own citations directly. No citation content changed, no new theological claim — a rendering-correctness fix, logged here rather than in `docs/theological-review-log.md` for that reason.
- **Bright-line gate expanded from three items to five, drawing on the Catechism of the Catholic Church and Evangelium Vitae.** `principles/non-negotiables.yaml` now also cites CCC §1867 (the "sins that cry to heaven") and Evangelium Vitae §3 (quoting *Gaudium et Spes* §27), both verified directly against the primary text. Two new items: `systemic-wage-theft-by-design` and `facilitation-of-trafficking-or-sexual-exploitation`. The existing `direct-killing-of-the-innocent` item picked up a CCC 1867 citation ("the blood of Abel") rather than a new duplicate item. CCC 1867's material on oppression of the poor/foreigner was deliberately *not* turned into gate items — it belongs in the graded principles, which admit mitigation the way the gate doesn't. Added `docs/ccc-citation-map.md`. Updated `README.md`, `rubric/criteria.md`, `CHECKLIST.md`, and `.claude/skills/rate-ai-against-cst/SKILL.md` for the new item count. **Unreviewed — beta**; see `docs/theological-review-log.md`, which also flags the trafficking item's "sin of the Sodomites" citation as needing explicit theological-reviewer attention given more than one plausible reading of the underlying narrative.
- **Added interaction auditing alongside use-case rating; renamed the skill to `rate-ai-against-cst`.** The assessment tool now takes either of two subjects through the identical two-stage rubric: a planned/described AI use (as before), or an actual `prompt` + `response` pair from a deployed LLM, audited after the fact — "is this specific response in line with CST," not just "is this use case." `eval/report.py` gained a `Subject` dataclass (`kind: "use_case" | "llm_interaction"`) that both the bright-line and graded rendering now key off of; `eval/assessment.py` validates that exactly one subject shape is given (never both, never neither, never `prompt` without `response`). `.claude/skills/rate-ai-use-against-cst/` renamed to `.claude/skills/rate-ai-against-cst/` and its `SKILL.md` now branches at the start between interviewing for a use case and collecting a verbatim prompt/response to audit — for an audit, both the bright-line check and the graded scores judge what the response actually said or did, not what the prompt asked for. `rubric/criteria.md` and the README updated accordingly.
- **Replaced the target/rater eval harness with a direct AI-use assessment tool.** The project's original shape — point a harness at a built AI system, run it through pre-written scenarios, score its responses via an LLM rater — didn't match what this project is actually for: rating a *described* AI use case against CST before or while it's built, the way a DPIA works. Removed `eval/harness.py`, `eval/loading.py`, `eval/scenario.py`, `eval/raters/` (and the `llm`/`anthropic` optional dependency it required), `rubric/scenarios/`, `integrations/ci-advisory-check.yml`, and `.claude/skills/run-cst-eval-harness/`. Added: `principles/non-negotiables.yaml` (a bright-line gate — direct abortion, euthanasia/assisted suicide, direct killing of the innocent — cited to Compendium §155 and Magnifica Humanitas §55, both verified against the primary text), `eval/principles.py` and `eval/assessment.py` (loads and validates a written assessment against real principle/non-negotiable ids; makes no LLM calls itself), a rewritten `eval/report.py`, and a project-local Claude Skill (interviews the user, does the CST judgment in conversation grounded directly in `principles/*.yaml`, then calls `eval.assessment` to validate and render — see the entry above for its current name and shape). `rubric/criteria.md` now documents the real two-stage rubric this implements — bright-line check first, then a graded 1-5 score with mitigations. **Unreviewed — beta**, including `non-negotiables.yaml`; see `docs/theological-review-log.md`.
- Added `.claude/skills/run-cst-eval-harness`, a project-local Claude Skill wrapping `eval.harness` so a non-technical user can get a real advisory report without hand-writing target/rater wiring — `scripts/setup_venv.sh` (robust Python 3.12+ venv setup), `scripts/scaffold_target.py` (writes demo or real target/rater boilerplate, with tests), and `references/` (writing a target, full CLI reference, how to read a report back in plain language). Lives under `.claude/skills/` rather than this repo's usual `skills/` convention so Claude Code actually discovers it. Validates cleanly with `skills-ref`. **Retired** — see the entry above.
- Added `principles/social-justice.yaml` as an eighth principle, cited to *Magnifica Humanitas* §77-81, which names "the principle of social justice" as one of its own five headline principles — not previously one of this project's seven. `preferential-option-for-the-poor.yaml` updated to cross-reference it. "Seven principles" updated to "eight" throughout README, CHECKLIST, and Definition of Done. Still **unreviewed** — beta. See `docs/magnifica-humanitas-findings.md`.
- All seven `principles/*.yaml` enhanced with *Magnifica Humanitas* citations alongside the existing Compendium ones (paragraph numbers verified directly against the primary text). Notable additions: `subsidiarity.yaml` now cites MH §71's explicit extension of subsidiarity to AI platforms; `universal-destination-of-goods.yaml` cites MH §67's explicit naming of algorithms, data, and digital platforms as goods the principle covers. See `docs/magnifica-humanitas-findings.md` for the full findings, including an open question about whether MH's "principle of social justice" (§77-81, not currently one of this project's seven) should become an eighth principle. Still **unreviewed** — beta.
- All seven `principles/*.yaml` drafted (citations, description, tensions, scenarios), sourced strictly from the Compendium of the Social Doctrine of the Church. Paragraph numbers were verified directly against the primary text. **Unreviewed** — written during the beta pause of the `CODEOWNERS` sign-off requirement; no *Magnifica Humanitas* citations yet (Compendium only), no theological review yet.
- `CODEOWNERS`'s theological-reviewer sign-off requirement for `principles/` and `rubric/` is paused for the beta phase, to be reimposed before leaving beta — see `CONTRIBUTING.md` "Beta status" and `docs/theological-review-log.md`.
- Eval harness v0.1 built: loads scenarios from `rubric/scenarios/*.yaml`, runs them against a pluggable target (`--target module:callable`), scores responses via pluggable multi-rater consensus (`--rater`, defaulting to a reference `AnthropicRater`), flags contested or disagreed-upon scenarios for human review instead of forcing a grade, and writes a timestamped advisory Markdown report. No theological content change — this is harness code only.
- Repo scaffolded: schema, rubric, and eval harness directories in place; no principle content written yet.
